-title "Zmiana hasła"
%h1 Zmiana hasła
=render 'slider'
%p
  Zalogowany jako
  =current_user.username + '.'
-form_for @account, :url => update_profile_path do |f|
  Obecne hasło
  %p
    %br
    = f.text_field :password
    Aby zatwierdzić zmiany wpisz hasło
  %p
    %br
    = f.text_field :email
    Email

  = f.submit "Gotowe"

