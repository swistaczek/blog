-title "Zmiana hasła"
%h1 Zmiana hasła
=render 'slider'
%p
  Zalogowany jako
  =current_user.username + '.'
-form_for @account do |f|
  Obecne hasło
  %br
  = f.text_field :username
  = f.submit "Gotowe"

